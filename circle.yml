machine:
  node:
    version: 4.0.0
dependencies:
  override:
    - npm install
deployment:
  prod:
    branch: master
    commands:
      - npm start
      - aws s3 sync build s3://chocolate-free.com --delete
